
npm run dev


Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.



Note to add new admin user you need to paste there uid in to the script in the scripts file run the file and then remove the uid from the script file. To run the file run the command ```node scripts/setAdminClaim.mjs``` from the root of the directory to add admin credentials to a user
